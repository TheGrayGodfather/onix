import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            dark: "#D08C00",
            main: "#FCA311",
            light: "#FFD699"
        },
        secondary: {
            dark: "#0E172B",
            main: "#14213D",
            light: "#6A7EAB"
        }
    }
})

export default theme